#include <bits/stdc++.h>
using namespace std;

#define ll long long
#ifdef LOCAL
#include "debug.hpp"
#else
#define dbg(...) __VA_ARGS__
#endif

constexpr int INF=0x3f3f3f3f;
constexpr int MAX=1000;

vector<int> v = {1,2,3};

int main(void){
    // Use This to inspect container values in local machine
    dbg(v);

    
    return 0;
}